<?php


 foreach ($file as $line) {
	 if (strpos($line, "%table%") === 0)  /
		 
		 /* 
		  * Append to .css file:
	  * div table .table-col-percent-p1-p2-p3-etc {  }
	   add_css(); 
           add_div(); // <div class="table-x%-y%-z%-etc"> 
             
 }
