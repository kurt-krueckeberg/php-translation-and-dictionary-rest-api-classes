# CSs Grid Design

When using the grid layout, you can add a gridline between rows, so perhaps you can also add it to the bottom of the section. This will add a divider that is visible between the sections or DIV's for each word, it's definitions and sample sentences


Grid-column-start and grid column are CSS. Grid properties. That can be used to display a line. Commis a comma at the start of a section. And at the end of the section, 

 The link below is an excellent explanation with explanation with several illustrations of grids and their associated settings.

https://www.w3schools.com/css/css_grid.asp



